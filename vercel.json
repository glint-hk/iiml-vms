{
  "version": 2,
  "buildCommand": "npm install && npm run build --workspace=client && cd server && npx prisma generate && npx prisma db push && node prisma/seed.js",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.js" }
  ]
}
